import styled from "@emotion/styled";

const Home = () => {
  return (
    <article>
      <Title>관리자 홈</Title>
    </article>
  );
};

const Title = styled.h2`
  margin-bottom: 30px;
  font-size: 30px;
  text-align: center;
`;

export default Home;
